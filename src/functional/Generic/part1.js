"use strict";
// 숫자를 그대로 돌려주는 함수 
const idNumber = (n) => {
    return n;
};
// 문자열을 그대로 돌려주는 함수
const idString = (n) => {
    return n;
};
// boolean 값을 그대로 돌려주는 함수
const idBoolean = (n) => {
    return n;
};
// 어떤 타입의 값이라도 그대로 돌려주는 함수
const id = (x) => {
    return x;
};
